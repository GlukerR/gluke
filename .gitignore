# Nuxt dev/build outputs
.output
.data
.nuxt
.nuxt-build
.nuxt-typecheck
.nitro
.cache
dist
.vite
*.tsbuildinfo

# Deploy provider artifacts
.vercel
.netlify
.wrangler

# Node dependencies
node_modules
.pnpm-store
.eslintcache

# Logs
logs
*.log
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Agent workspace
.freebuff

# Editors and OS
.DS_Store
Thumbs.db
desktop.ini
.fleet
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Local env files
.env
.env.*
!.env.example

# WIP: лава-лампа из метаболлов — кейс снят с публикации. Скрываем только
# сам кейс: без своего .md он нигде не появляется — ни страницей, ни
# карточкой в каталоге, ни в sitemap. Исходники виджета, наоборот, должны
# лежать в репозитории: на них статически ссылаются ProjectCardDemo.vue и
# ProjectDetailHero.vue, и без них сборка падает с TS2307.
content/projects/ru/metaballs.md
content/projects/en/metaballs.md
public/media/projects/metaballs/

# Локальный тулинг агентов (launch.json и т.п.) — не для коммита
.claude/
