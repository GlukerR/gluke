{
  "framework": "nuxtjs",
  "installCommand": "pnpm install",
  "buildCommand": "rm -rf node_modules/.cache/nuxt && if git fetch --unshallow; then pnpm lastmod -- --all || echo \"[lastmod] warning: refresh failed, keeping committed dates\"; else echo \"[lastmod] skipped: no full git history on Vercel\"; fi && pnpm build",
  "outputDirectory": ".output",
  "regions": ["fra1"],
  "headers": [
    {
      "source": "/media/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=3600, stale-while-revalidate=86400, stale-if-error=604800"
        }
      ]
    }
  ]
}
